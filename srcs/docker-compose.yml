version: "11"

services:
    nginx:
        container_name: nginx
        image:          nginx
        build:          ./requirements/nginx/
        env_file:       .env
        restart:        on-failure
        ports:
            - "443:443"
        # networks:
        #     - inception
        # volumes:
        #     - wordpress-data:/var/www/html/
        # depends_on:
        #     - wordpress
